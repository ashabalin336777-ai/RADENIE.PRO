# Положите сюда сертификаты Timeweb (НЕ коммитьте в Git):
#
#   radenie.pro.crt  — сертификат (CRT), при необходимости полный chain
#   radenie.pro.key  — приватный ключ (Private Key)
#
# Права на ключ: chmod 600 radenie.pro.key
#
# Если Timeweb выдал отдельный CA/chain — склейте в один файл:
#   cat certificate.crt ca_bundle.crt > radenie.pro.crt
